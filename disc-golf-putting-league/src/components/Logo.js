import React from 'react'; // Import React library

// Functional component definition
const Logo = () => {
  // Component logic (e.g., state, event handlers)

  return (
    <div>
      {/* JSX (HTML-like syntax) */}
      <img className='logo' alt='' src='/img/Winter Putting League-01.png'></img>
    </div>
  );
};

export default Logo